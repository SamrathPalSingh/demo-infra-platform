apiVersion: demo.contracts/v1
kind: NetworkState
metadata:
  name: shared-egress-and-ingress
  # This shared network state is intentionally stable for the contract-check demo.
spec:
  nat:
    allocation_method: static
    addresses:
      - 34.120.10.10
      - 34.120.10.11
  ingress:
    class: public-nginx
    address: 34.98.20.5
    tls: enabled
